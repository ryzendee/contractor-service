version: '3.9'

services:
  contractor-db:
    image: postgres:16.9-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=contractordb
    ports:
      - "5311:5432"
